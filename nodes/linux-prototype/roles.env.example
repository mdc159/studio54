# Roles enabled on this node. See stack/topology/roles.json for the full
# role vocabulary. Copy to `roles.env` (gitignored if you override here)
# or source from your shell profile.
ENABLED_ROLES=core,media-cpu,tools
